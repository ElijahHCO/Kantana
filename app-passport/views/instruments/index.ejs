<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <% if (messages.error) { %>
        <%= messages.error %>
      <% } %>

    <% if(instruments.length === 0) {%>
        <h2>There are no instruments yet!</h2>
        <a href="/instruments/new">Create one now!</a>
    <% } else { %> 
    <ul class="instruments">
        <% instruments.forEach((instrument)=>{ %>
          
                <li><a href="/instruments/<%=instrument._id%>"><%= instrument.name %><%= instrument.username.username %></a> is only $<%=instrument.price%>, in <%=instrument.location%> </li>
            <% }) %>
     
    </ul>
    <% } %>


    <a href="/users/home"><button>Go Back Home</button></a>

    <a href="/instruments/new"><button>add new instrument</button></a>
</body>
</html>